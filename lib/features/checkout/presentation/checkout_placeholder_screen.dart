import 'package:flutter/material.dart';

class CheckoutPlaceholderScreen extends StatelessWidget { const CheckoutPlaceholderScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Checkout Placeholder')), body: const Center(child: Text('TODO: Integrasi checkout, ongkir, dan pembayaran'))); }
