import 'package:flutter/material.dart';

class EditorPlaceholderScreen extends StatelessWidget { const EditorPlaceholderScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Editor Placeholder')), body: const Center(child: Text('TODO: Editor PhotoBook tahap berikutnya'))); }
